# FeedParser 测试数据

此目录用于存放 RSS 测试文件。

## 文件格式

请将 RSS 2.0 格式的 XML 文件放在此目录中，文件扩展名为 `.xml`。

## 示例

将真实的 RSS Feed 保存为 XML 文件，例如：

- `bbc-news.xml` - BBC 新闻 RSS
- `tech-blog.xml` - 技术博客 RSS

## 测试

本地集成测试会自动遍历此目录中的所有 `.xml` 文件进行解析测试。
